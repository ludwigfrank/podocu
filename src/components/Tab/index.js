import React from 'react'
import styled from 'styled-components'

export default class Tab {
    state = {
        activeTab: 0
    }

    render () {
        return (
            <div> Tab </div>
        )
    }
}